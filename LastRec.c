/*count the number of digits with recursion*/


#include<stdio.h>
void dig (int x)
{
    if (x%10 == 0)
}
void main ()
{
    int num = 0;printf("Enter a Number :");
    scanf("%d",&num);
    dig (num);
    }